1.class and object 
- class is the blue print of model. it is just like a black page
- an object is the instance - when class is created no memory is allocted but when class is created momory
- is allcated in the stroage data.
- you can create multiple object from a single class 
2. why python is called oops?
3. make a code of constructor it is simple project on game 
4. make a simple chatbot program
- user make there signup then signin then write message then post there message to there friend 
5. push to main git 
6. Encapsulation it means that make method private that is not used directly from outside the class
- it restrict access to certain attributes or method to protect data enforced controll access
7. Abstraciton- it mean that hidding unnessory details from user friend
- u can access it by making mthod variable 
8. Polymorphism- simple word of polymorphis is many forms
- so it may be define as it allow the same function,operator object to behave differently 
-on the context
-as classroom you behave like student 
- in store you behave as a customer 
- at home, you behave as child
-Inheritance it means that allow one class (child) to reuse the properties and method of another
- class(parents)
